import {View, Text, Button} from 'react-native';
import FieldHandler from '../components/Field/FieldHandler';

export default function Strategy () {

    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <FieldHandler/>
        </View>
    )
}